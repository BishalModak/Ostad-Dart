main(){
  final DateTime nowDateTime; //runtime time
  const double pi = 3.1416; // compile  time;

  nowDateTime = DateTime.now();
  print(nowDateTime);


// Const: Represents things that are always the same, like a fixed ticket price. These values are known at compile-time.
// Final: Represents things that can be calculated or determined later, like dynamic ticket pricing. These values are decided at runtime but don't change afterward.


}